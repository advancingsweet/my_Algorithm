# 海港



```C++ 
#include<iostream>
#include<algorithm>
#include<cstdio>
#include<cstring>

/*
    输入数据n
    
    输入数据t,k, 以及k 个乘客的信息
    
    依次遍历时间t，找满足要求的最小时间T(满)；  // 利用二分查找
    
    统计 T(满) ~ t 时间段内， 有多少国家的旅客
    

*/


using namespace std;

const int N = 100010,M = 300010;
typedef long long ll;

ll t[N],tt[N];  //tt表示 第i时刻进入的数据
ll s[N][N],p[N];
int n,k;
int head = 0,rear = -1;

int Traceback(int m)  // 传第m条船
{
    ll left = 0, right = n,mid;
    if(tt[T[m]] - tt[T[1]] <= 0) return 1; // 直接返回第一条船
    while(left + 1 != right) // 利用二分法找到第一个满足条件的数
    {
        mid = left + right >> 1;
        int x = tt[T[m]] -  86400;
        if(tt[T[mid]] > x) mid = right;  // right 就为第一个刚好
        else mid = left;
    }
    return right;
}

int main()
{
    cin>>n; // n 搜船
    
    int x;
    
    for(int i = 1;i <= n;i ++)
    {
        scanf("%d%d",&x,&k); // 输入船入港时间、船上的人数
        
        T[i] = t; // 第i艘船抵达时间为t
        
        tt[i] = k; // 在第i艘船上的人数为k;
        
        for(int j = 1;j <= k;j ++)
        {
            scanf("%d",&x);
            s[x][j] = x;   // 数据录入
        }
    }
    
    for(int i = 1;i <= n; i ++)
    {
        int numb = Traceback(i);  // 时间区间找到
        
        for(int j = numb ;j <= i;j ++)
            for(int m = 1; m <= s[j][])
        
        
        
        
    }
    
    
    reutrn 0;
}
```

